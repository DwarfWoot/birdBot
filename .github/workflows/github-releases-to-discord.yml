on:
  release:
    types: [published]

jobs:
  build:
    # The type of runner/virtual machine the job will run on
    runs-on: ubuntu-latest

    steps:
    # Step 1: Check out the repository code so the workflow can access it
      - name: Check out repository
        uses: actions/checkout@v4

    # Step 2: Set up the Python environment
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11' # Specify your preferred Python version

    # Step 3: Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

#  github-releases-to-discord:
#    runs-on: ubuntu-latest
#    steps:
#      - name: Checkout
#        uses: actions/checkout@v3
#      - name: GitHub Releases to Discord
#        uses: SethCohen/github-releases-to-discord@v1
#        with:
#          webhook_url: ${{ secrets.WEBHOOK_URL }}
#          color: "2105893"
#          username: "catBot on github"
#          avatar_url: "https://cdn.discordapp.com/attachments/1287848895954681947/1358850483275698236/Tang.png?ex=6a7f4b21&is=6a7df9a1&hm=96e4f3aba7b99f048a0368daa6d3e961103c9d26a65f07eb55f36aa00bd74633"
#          content: "<@&1537515195151155230>"
#          footer_title: "Changelog"
#          reduce_headings: false
